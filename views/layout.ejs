<!DOCTYPE html>
<html lang="it">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>NEXUS CRM</title>
  <!-- Tailwind CSS -->
  <link rel="stylesheet" href="/css/styles.css">
  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css">
</head>
<body class="bg-gray-50 min-h-screen flex flex-col">
  <% if (user) { %>
    <!-- Layout con sidebar -->
    <div class="flex h-screen overflow-hidden">
      <!-- Sidebar mobile overlay -->
      <div id="sidebar-overlay" class="fixed inset-0 bg-black opacity-50 z-20 hidden lg:hidden"></div>
      
      <!-- Sidebar -->
      <aside id="sidebar" class="bg-nexus-dark text-white w-64 flex-shrink-0 fixed inset-y-0 left-0 transform -translate-x-full lg:translate-x-0 z-30 lg:static lg:z-auto transition duration-200 ease-in-out overflow-y-auto">
        <div class="flex flex-col h-full">
          <!-- Logo -->
          <div class="py-4 px-6 border-b border-gray-700">
            <a href="/" class="text-nexus-red font-bold text-xl">NEXUS</a>
            <p class="text-sm text-gray-400">Sistema CRM</p>
          </div>

          <!-- Menu -->
          <nav class="flex-1 py-4 px-4">
            <ul class="space-y-1">
              <li>
                <a href="/" class="sidebar-link">
                  <i class="fas fa-tachometer-alt w-5 text-center mr-2"></i> Dashboard
                </a>
              </li>
              
              <!-- Menu Clienti con sottomenu -->
              <li>
                <div class="sidebar-menu-group">
                  <a href="/clients" class="sidebar-link sidebar-parent">
                    <i class="fas fa-users w-5 text-center mr-2"></i> Clienti
                    <i class="fas fa-chevron-down ml-auto transition-transform duration-200" id="clients-chevron"></i>
                  </a>
                  <ul class="sidebar-submenu hidden" id="clients-submenu">
                    <li>
                      <a href="/clients/new" class="sidebar-sublink">
                        <i class="fas fa-plus w-4 text-center mr-2 text-green-400"></i> Nuovo Cliente
                      </a>
                    </li>
                    <li>
                      <a href="/clients/import-bill" class="sidebar-sublink">
                        <i class="fas fa-file-import w-4 text-center mr-2 text-blue-400"></i> Importa da Bolletta ENEL
                      </a>
                    </li>
                  </ul>
                </div>
              </li>
              
              <li>
                <a href="/activities" class="sidebar-link">
                  <i class="fas fa-tasks w-5 text-center mr-2"></i> Attività
                </a>
              </li>
              <li>
                <a href="/activities/calendar" class="sidebar-link">
                  <i class="fas fa-calendar w-5 text-center mr-2"></i> Calendario
                </a>
              </li>
              <li>
                <a href="/contracts" class="sidebar-link">
                  <i class="fas fa-file-contract w-5 text-center mr-2"></i> Contratti
                </a>
              </li>
              <li>
                <a href="/contracts/stats" class="sidebar-link">
                  <i class="fas fa-chart-bar w-5 text-center mr-2"></i> Statistiche
                </a>
              </li>
              <li>
                <a href="/utilities/expiring-contracts" class="sidebar-link">
                  <i class="fas fa-exclamation-triangle w-5 text-center mr-2"></i> Contratti in Scadenza
                </a>
              </li>
              <li>
                <a href="/supply-points" class="sidebar-link">
                  <i class="fas fa-plug w-5 text-center mr-2 text-green-400"></i> Punti di Fornitura
                </a>
              </li>
              <% if (user.role === 'administrator') { %>
                <li>
                  <a href="/users" class="sidebar-link">
                    <i class="fas fa-user-cog w-5 text-center mr-2"></i> Utenti
                  </a>
                </li>
                <li>
                  <a href="/products" class="sidebar-link">
                    <i class="fas fa-box w-5 text-center mr-2"></i> Listino Prodotti
                  </a>
                </li>
              <% } %>
            </ul>
          </nav>

          <!-- Footer -->
          <div class="border-t border-gray-700 p-4">
            <div class="text-xs text-gray-400">
              <p>NEXUS CRM &copy; <%= new Date().getFullYear() %></p>
              <p>Sistema gestione consulenza energetica</p>
            </div>
          </div>
        </div>
      </aside>

      <!-- Main content -->
      <div class="flex-1 flex flex-col overflow-hidden">
        <!-- Top navbar -->
        <header class="bg-nexus-dark text-white">
          <div class="flex items-center justify-between h-16 px-6">
            <!-- Mobile menu button -->
            <button id="mobile-menu-button" class="lg:hidden">
              <i class="fas fa-bars text-white"></i>
            </button>

            <!-- User actions -->
            <div class="flex-1 flex justify-end items-center">
              <div class="flex-shrink-0 text-white mr-4">
                <i class="fas fa-user mr-1"></i> <%= user.name %> 
                <span class="<%= user.role === 'administrator' ? 'badge-red' : 'badge-gray' %> ml-2">
                  <%= user.role === 'administrator' ? 'Admin' : 'Consulente' %>
                </span>
              </div>
              <a href="/logout" class="btn-outline-red btn-sm">
                <i class="fas fa-sign-out-alt"></i> Logout
              </a>
            </div>
          </div>
        </header>

        <!-- Page content -->
        <main class="flex-1 overflow-x-hidden overflow-y-auto p-6 bg-gray-50">
          <%- body %>
        </main>
      </div>
    </div>
  <% } else { %>
    <!-- Contenuto principale per utenti non autenticati -->
    <main class="container mx-auto px-4 py-8 flex-grow">
      <%- body %>
    </main>

    <!-- Footer -->
    <footer class="bg-nexus-dark text-white text-center py-3">
      <div class="container mx-auto">
        <small>NEXUS CRM &copy; <%= new Date().getFullYear() %> - Sistema di gestione consulenza energetica</small>
      </div>
    </footer>
  <% } %>

  <script src="/js/main.js"></script>
  
  <!-- Script per gestione sottomenu sidebar -->
  <script>
    document.addEventListener('DOMContentLoaded', function() {
      // Gestione sottomenu Clienti
      const clientsParent = document.querySelector('.sidebar-parent');
      const clientsSubmenu = document.getElementById('clients-submenu');
      const clientsChevron = document.getElementById('clients-chevron');
      
      if (clientsParent && clientsSubmenu && clientsChevron) {
        // Controlla se siamo in una pagina del sottomenu per aprirlo automaticamente
        const currentPath = window.location.pathname;
        if (currentPath.includes('/clients/') || currentPath === '/clients/new') {
          clientsSubmenu.classList.remove('hidden');
          clientsChevron.style.transform = 'rotate(180deg)';
        }
        
        clientsParent.addEventListener('click', function(e) {
          // Se non è il link principale /clients, previeni la navigazione
          if (!e.target.href || !e.target.href.endsWith('/clients')) {
            e.preventDefault();
          }
          
          // Toggle del sottomenu
          clientsSubmenu.classList.toggle('hidden');
          
          // Rotazione della chevron
          if (clientsSubmenu.classList.contains('hidden')) {
            clientsChevron.style.transform = 'rotate(0deg)';
          } else {
            clientsChevron.style.transform = 'rotate(180deg)';
          }
        });
        
        // Evidenzia il link attivo nel sottomenu
        const subLinks = clientsSubmenu.querySelectorAll('.sidebar-sublink');
        subLinks.forEach(link => {
          if (link.href === window.location.href) {
            link.classList.add('active');
          }
        });
      }
    });
  </script>
</body>
</html>