<% if (product.additional_cost1_description || product.additional_cost2_description || product.additional_cost3_description) { %>
<div class="bg-yellow-50 p-4 rounded-lg mb-6">
  <h3 class="text-lg font-medium text-yellow-900 mb-4">
    <i class="fas fa-plus-circle mr-2"></i>Costi Aggiuntivi
  </h3>
  
  <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
    <% if (product.additional_cost1_description) { %>
    <div class="bg-white p-3 rounded border-l-4 border-yellow-500">
      <div class="text-sm font-medium text-gray-500"><%= product.additional_cost1_description %></div>
      <div class="text-lg font-bold text-yellow-600">
        €<%= product.additional_cost1_value %> <%= product.additional_cost1_unit %>
      </div>
    </div>
    <% } %>
    
    <% if (product.additional_cost2_description) { %>
    <div class="bg-white p-3 rounded border-l-4 border-yellow-500">
      <div class="text-sm font-medium text-gray-500"><%= product.additional_cost2_description %></div>
      <div class="text-lg font-bold text-yellow-600">
        €<%= product.additional_cost2_value %> <%= product.additional_cost2_unit %>
      </div>
    </div>
    <% } %>
    
    <% if (product.additional_cost3_description) { %>
    <div class="bg-white p-3 rounded border-l-4 border-yellow-500">
      <div class="text-sm font-medium text-gray-500"><%= product.additional_cost3_description %></div>
      <div class="text-lg font-bold text-yellow-600">
        €<%= product.additional_cost3_value %> <%= product.additional_cost3_unit %>
      </div>
    </div>
    <% } %>
  </div>
</div>
<% } %>
